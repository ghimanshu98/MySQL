use employees;

SELECT 
    first_name, last_name
FROM
    employees;
    
SELECT 
    *
FROM
    employees;
    
-- Select the information from the “dept_no” column of the “departments” table.

SELECT 
    dept_no
FROM
    departments;